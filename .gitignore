.memhub/
agent_docs/PROJECT.md
agent_docs/PROJECT_LEDGER.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Probe output — local scan data, regenerated on every run
ysfprobe.db

# Raw response cache — third-party dashboard HTML, local scan data belonging
# to the operator, must not be redistributed
ysfprobe_cache/

# Registry data fetched at runtime. Belongs to RefCheck.Radio, not this repo —
# their terms prohibit redistribution with altered attribution.
YSFHosts.*

# Agent guidance — kept local, deliberately not published
CLAUDE.md
.orchestrator/worktrees/
.orchestrator/config.local.toml
.orchestrator/state.json
.orchestrator/delivery.lock
