# Dev-only dependencies for running the test suite. The runtime dependency
# (httpx) is pinned in requirements.txt, not here; pytest is never imported
# by ysfprobe.py itself. Install both files together to run the suite:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=7
